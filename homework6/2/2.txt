2. Проанализируйте задания предыдущих уроков.
a. В каких случаях необходима была явная передача указателя в качестве входных параметров 
и возвращаемых результатов или в качестве приёмника в методах?

При использовании методов fmt.Scan, fmt.Scanln

b. В каких случаях мы фактически имеем дело с указателями при передаче параметров, хотя явно их не указываем?

При передаче слайса во входные параметры функции т.к. слайс содержит указатель на массив элементов, с которым мы работаем
Пример: https://github.com/mulla159/gb-go-step-1/blob/main/homework4/main.go
